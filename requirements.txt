fastapi
uvicorn
websockets>=16.0
dnspython>=2.6.0
email-validator>=2.0.0
sqlalchemy
psycopg2-binary>=2.9.9
python-jose
python-multipart
alembic
passlib>=1.7.4
cloudinary
bcrypt>=4.0.1,<5
python-dotenv
python-multipart
stripe>=10.0.0
httpx>=0.27.0
cryptography>=42.0.0
google-api-python-client>=2.130.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
redis>=5.0.0
rq>=1.16.0
reportlab>=4.0.0
opentimelineio>=0.15.0
Pillow>=10.0.0

# Cloudflare R2 (S3-compatible) storage
boto3>=1.34

# Claude (AI creative director). Pure Python, no native deps, so unlike the
# heavier model runtimes in requirements-ml.txt it belongs here: the director's
# API surface is part of the core app, not an optional worker extra. The import
# itself is still lazy (see app/services/claude_client.py), so a deployment
# without a key never pays for it.
anthropic>=0.75
