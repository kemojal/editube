- review playback security: signed proxy URLs hide permanent storage links; screen capture / “Save as” while a playback URL is valid is still possible. True DRM would need encrypted packaging (e.g. HLS + Widevine/FairPlay/ClearKey) and a license policy — backlog if clients require it.

- comment panels
    - comment like/ reaction not working 
    - commen reply input text areay too big / also reply should be visibvle to the replied user 


-- add internalization ( english, taiwan madarin chinese , japanese )

-- move the storage to cloudflare r2
-- add forum/feedback future list
-- review not only video but also file documents like pdf,...



-- billing and pricing 
-- freelancer/youtuner/ asgency should have different interfaces and wrksapce flow 

-- in the create link can share the link with team members ..... 

--ux/ui
  - option to pin a project also show recent projects
  - colaboration/ invite users/collaraboratoes
  -- when like expired before routing user to login lets save user prevoous page and when user relogin we take him their straign
  -- show videos/assets thumbnail
  --hook the webscokt and notication to the actiosn like mentions, ...... 
  

